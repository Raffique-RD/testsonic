version: 2.1
jobs:
  report-sender:
    machine:
      image: "ubuntu-2004:current"
    steps:
      - checkout
      - run: |
          ls
          docker pull aldanial/cloc
          docker pull owasp/dependency-check
          docker cp ./. aldanial/cloc:/tmp/
          docker exec -it aldanial/cloc 
          /usr/src/cloc /tmp
          ls
          exit
          ls
workflows:
  tasks:
    jobs:
      - report-sender
